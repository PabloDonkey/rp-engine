# Application
RP_ENGINE_APP_NAME=RP Engine
RP_ENGINE_APP_ENVIRONMENT=development
RP_ENGINE_APP_HOST=0.0.0.0
RP_ENGINE_APP_PORT=8000
RP_ENGINE_LOG_LEVEL=INFO
RP_ENGINE_DEBUG_STATUS_ENABLED=false
RP_ENGINE_PERSISTENCE_BACKEND=json
RP_ENGINE_DEFAULT_WORLD_ID=default

# PostgreSQL (application)
RP_ENGINE_POSTGRES_HOST=localhost
RP_ENGINE_POSTGRES_PORT=5432
RP_ENGINE_POSTGRES_DATABASE=rp_engine
RP_ENGINE_POSTGRES_USER=rp_engine
RP_ENGINE_POSTGRES_PASSWORD=change_me
RP_ENGINE_POSTGRES_SSL_MODE=disable
RP_ENGINE_POSTGRES_POOL_SIZE=10
RP_ENGINE_POSTGRES_MAX_OVERFLOW=10

# PostgreSQL (docker compose)
POSTGRES_DB=rp_engine
POSTGRES_USER=rp_engine
POSTGRES_PASSWORD=change_me

# Telegram
RP_ENGINE_TELEGRAM_ENABLED=true
RP_ENGINE_TELEGRAM_BOT_TOKEN=
RP_ENGINE_TELEGRAM_MESSAGE_MAX_LENGTH=3800
RP_ENGINE_TELEGRAM_ADMIN_USER_ID=
RP_ENGINE_TELEGRAM_AUTHORIZATION_DIR=data/telegram/authorization
# Optional override; defaults to a closed-beta message pointing users at /beta.
# RP_ENGINE_TELEGRAM_UNAUTHORIZED_MESSAGE=

# LM Studio
RP_ENGINE_LMSTUDIO_API_HOST=localhost:1234
RP_ENGINE_LMSTUDIO_MODEL=atlantis-v0.1-12b@q6_k
# Max tokens per reply. 0 (or absent / empty) means no limit.
RP_ENGINE_LMSTUDIO_MAX_TOKENS=1000
RP_ENGINE_LMSTUDIO_TEMPERATURE=0.8

# LM Studio Sampling Parameters
RP_ENGINE_LMSTUDIO_TOP_K_SAMPLING=40
RP_ENGINE_LMSTUDIO_REPEAT_PENALTY=1.1
RP_ENGINE_LMSTUDIO_TOP_P_SAMPLING=0.95
RP_ENGINE_LMSTUDIO_MIN_P_SAMPLING=0.05

RP_ENGINE_DEBUG_GENERATION_TRACE=off

# Scenarios
# Comma-delimited list of catalog directories, merged in order. On a scenario id
# collision across directories, the later directory wins (e.g. a local/private
# catalog listed after the curated one can override a curated scenario).
RP_ENGINE_SCENARIO_CATALOG_DIRS=data/catalog

# pgAdmin (docker compose)
PGADMIN_DEFAULT_EMAIL=admin@local.dev
PGADMIN_DEFAULT_PASSWORD=change_me
